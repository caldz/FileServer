仓库地址】
https://github.com/caldz/FileServer.git

创建SSH指令】
ssh-keygen -t rsa -C "136820607@qq.com"

提交指令】
git commit -a -m "update"

上传指令】
git push https://github.com/caldz/FileServer.git

同步指令】有时候需要先同步才能上传
git pull

拉取远程代码】
git clone https://github.com/caldz/FileServer.git

查询信息】
git config -l